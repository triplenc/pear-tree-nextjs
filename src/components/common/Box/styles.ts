import { styled } from "@stitches";

/**
 * margin, padding 값과 semantic tag 처리에 사용.
 * @as {string} semantic tag _스타일이 없는 semantic tag라면 original semantic을 사용해주세요._
 */
export const Box = styled("div", {
  m: 0,
  p: 0,
});
